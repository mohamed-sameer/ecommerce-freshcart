import { SearchPipPipe } from './search-pip.pipe';

describe('SearchPipPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPipPipe();
    expect(pipe).toBeTruthy();
  });
});
